<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plugin Store</title>
    <style>
        :root {
            {% for name, color in palette.items() %}
            --{{ name }}: {{ color }};
            {% endfor %}
        }
    </style>
    <link rel="stylesheet" href="appstore.css">
    <script src="qrc:///qtwebchannel/qwebchannel.js"></script>
</head>
<body>
    <div id="page-nav">
        <button class="page-btn active" data-page="explore" title="Everything available to install">Explore</button>
        <button class="page-btn" data-page="installed" title="What is installed in this workspace">Installed</button>
        <button class="page-btn" data-page="updates" title="Installed items with a newer version">Updates <span id="updates-count"></span></button>
    </div>

    <div id="toolbar">
        <input type="text" id="search-input" placeholder="Search plugins, workspaces, examples…" title="Search plugins, workspaces, examples">
        <select id="sort-select" title="Sort results">
            <option value="stars">Popular</option>
            <option value="name">Name</option>
        </select>
    </div>

    <div id="tag-chips"></div>

    <div id="loading">Loading…</div>

    <div id="hero"></div>

    <div class="cards-grid" id="package-cards"></div>

    <div id="detail-page" class="hidden">
        <div class="detail-top"><button id="detail-back" type="button">&larr; Back</button></div>
        <div id="detail-content"></div>
    </div>

    <div id="lightbox" class="hidden">
        <img id="lightbox-img" alt="">
    </div>

    <script src="appstore.js"></script>
</body>
</html>
