QProgressBar {
    background: palette(mid);
    color: palette(text);
    border: none;
    border-radius: 4px;
    text-align: center;
}

QProgressBar::chunk {
    background-color: {{ palette('highlight') }};
    border-radius: 4px;
}
